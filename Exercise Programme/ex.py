# try:
#     age = int(input("age: "))
#     income = float(input("income: "))
#     risk = income / age
#     print(age)
# # value error occur when when we provide a data that does not belong to that data type
# except ValueError:
#     print("Not a valid number")
# except ZeroDivisionError:
#     print("Age cannot be zero")
